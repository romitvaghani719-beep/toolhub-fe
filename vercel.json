{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install --prefix ../toolhub-be/shared && npm install --prefix ../toolhub-be && npm install",
  "buildCommand": "npm run build --prefix ../toolhub-be/shared && npm run build && npm run build --prefix ../toolhub-be",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/../toolhub-be/api/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "functions": {
    "../toolhub-be/api/**/*.ts": {
      "memory": 1024,
      "maxDuration": 10
    }
  }
}
