# ToolHub Frontend

Calls the backend API only — no Supabase configuration needed.

## Setup

```bash
npm install
```

Create `.env`:
```env
VITE_BE_URL=http://localhost:3001
```

## Run

Start the backend first (`../toolhub-be`), then:

```bash
npm run dev
```

See [SETUP.md](../toolhub-be/SETUP.md) for full instructions.
